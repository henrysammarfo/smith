# AO — what to do next (from your Orchestrator screen)

You already have:
- Agent Orchestrator open
- Project **smith** selected
- Coding agent available (OpenCode Zen / Big Pickle shown)

## Do this now

1. Keep AO open (sessions only count while the desktop app is running).
2. In the bottom box **Message the agent…**, send a real task, for example:

```
Open the SMITH repo. Run: bun install && bun run smoke
Then summarize Gen1 vs Gen2 accuracy and memory growth.
```

3. Start a second task after the first finishes:

```
Open http://localhost:3000/dashboard/forge (or the Vercel URL).
Walk Gen1 forge then Gen2 forge and note the score lift.
```

4. Leave AO running while you record the demo video so session count is visible.

## Do not

- Do not quit AO mid-demo
- Do not invent session counts
- Cloud AppImage does not count for judging

More setup: `docs/AO_DESKTOP_SETUP.md`
